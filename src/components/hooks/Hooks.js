//IMPORTACIONES

//Modulos
import { useState } from 'react'
//Estilos
import './Hooks.css'

//LOGICA
const Hooks = () => { //Funcion constructora
    
    /* //Hooks de estado
    //Constructor [Estado y funcion que actualiza el estado] = inicializacion del estado (el valor por default del estado)
    const [contador,setContador] = useState(0)

    const sumar = () => {
        setContador(contador + 1)
    }
    const restar = () => {
        setContador(contador - 1)
    }

    return (
        <div>
            <button onClick={sumar}>Sumar</button>
            <p></p>{contador}
            <button onClick={restar}>Restar</button>

        </div>
    ) */
}

//EXPORTACION
export default Hooks